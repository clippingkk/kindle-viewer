<Page
    x:Class="kindle_viewer.pages.Profile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:kindle_viewer.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <StackPanel>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0, 30,0,0"
                    BorderBrush="White"
                    BorderThickness="10"
                    >
            <Image
                Source="{x:Bind profile.Avatar, Mode=OneWay}" Width="150" Height="150"
                />
            <StackPanel VerticalAlignment="Center"
                        Background="Transparent"
                        >
                <TextBlock Text="{x:Bind profile.Email, Mode=OneWay}" />
                <TextBlock Text="{x:Bind profile.Name, Mode=OneWay}" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>
